let x = Math.floor(Math.random() * 100 + 1);
let y = Math.floor(Math.random() * 50 + 1);

sum = x + y;

sumOutput = `${x} + ${y} = ${sum}`;

diff = x - y;

diffOutput = `${x} - ${y} = ${diff}`;

product = x * y;

productOutput = `${x} * ${y} = ${product}`;

quotient = x / y;

quotientOutput = `${x} / ${y} = ${quotient}`;

reminder = x % y;

reminderOutput = `${x} % ${y} = ${reminder}`;



console.log(reminderOutput); 
